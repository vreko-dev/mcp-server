// lib/supabase/server.ts
// Note: @supabase/ssr needs to be installed separately
// Run: pnpm add @supabase/ssr

/**
 * Example implementation for Supabase server client
 * This is a placeholder until @supabase/ssr is installed
 */

export async function createClient() {
	// This would be implemented when @supabase/ssr is available
	throw new Error(
		"Supabase server client not implemented - @supabase/ssr not installed",
	);
}
