// TODO: Implement ActiveOrganizationProvider component
// Stub file to enable build while org API is being developed

import type { ReactNode } from "react";

export function ActiveOrganizationProvider({ children }: { children: ReactNode }) {
	return <>{children}</>;
}
