{
  "name": "snapback-mcp-server",
  "version": "2.0.0",
  "description": "SnapBack MCP Server - Streamable HTTP transport for AI assistant integration",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "node --loader ts-node/esm src/index.ts",
    "build": "tsup",
    "docker-build": "docker build -t snapback-mcp:${GIT_SHA:-latest} -f apps/mcp-server/Dockerfile .",
    "start": "node dist/index.js",
    "validate": "node scripts/validate-bundle.js",
    "validate:fix": "node scripts/validate-bundle.js --fix",
    "docker:test": "./scripts/docker-test.sh",
    "docker:shell": "./scripts/docker-test.sh --shell",
    "predeploy": "pnpm build && pnpm validate",
    "deploy": "./scripts/deploy-fly.sh",
    "deploy:dry-run": "./scripts/deploy-fly.sh --dry-run",
    "deploy:quick": "fly deploy . --config fly.toml --dockerfile Dockerfile --remote-only",
    "deploy:force": "fly deploy . --config fly.toml --dockerfile Dockerfile --remote-only --strategy immediate",
    "deploy:status": "fly status -a snapback-mcp",
    "deploy:logs": "fly logs -a snapback-mcp",
    "deploy:rollback": "fly releases -a snapback-mcp",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "type-check": "tsc --noEmit",
    "lint": "biome check .",
    "format": "biome format ."
  },
  "dependencies": {
    "@snapback/mcp": "workspace:*",
    "@snapback/platform": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "ts-node": "catalog:",
    "tsup": "catalog:",
    "vitest": "catalog:",
    "@vitest/coverage-v8": "catalog:"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
