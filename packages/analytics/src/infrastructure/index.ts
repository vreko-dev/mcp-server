/**
 * Analytics Infrastructure
 *
 * Re-exports infrastructure utilities for analytics use.
 * The PostHog utilities (alerts, cohorts, correlation) are already
 * available from @snapback/infrastructure, so we just re-export them here.
 */

export * from "@snapback/infrastructure";
