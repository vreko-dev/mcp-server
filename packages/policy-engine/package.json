{
  "name": "@snapback/policy-engine",
  "version": "0.0.1",
  "dependencies": {
    "@snapback/auth": "workspace:*",
    "@snapback/config": "^0.1.1",
    "@snapback/contracts": "^0.1.4",
    "@snapback/core": "workspace:*",
    "@snapback/events": "^0.1.1",
    "@snapback/infrastructure": "^0.1.2",
    "@snapback/integrations": "workspace:*",
    "@snapback/platform": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@snapback/tsconfig": "workspace:*",
    "@types/node": "catalog:",
    "tsup": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "private": true,
  "scripts": {
    "build": "tsup",
    "postbuild": "echo 'Package build complete'",
    "test": "test -d test && vitest run || echo 'No test directory found, skipping tests'",
    "test:coverage": "test -d test && vitest run --coverage || echo 'No test directory found, skipping tests'"
  },
  "type": "module",
  "types": "./dist/index.d.ts"
}
