{
  "name": "@snapback/standalone-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "catalog:",
    "@snapback/vitest-config": "workspace:*"
  }
}
