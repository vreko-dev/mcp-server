{
  "name": "@snapback/github-action",
  "version": "0.0.0",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1"
  },
  "devDependencies": {
    "@snapback/tsconfig": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "main": "index.js",
  "private": true,
  "scripts": {
    "test": "test -d test && vitest run || echo 'No test directory found, skipping tests'",
    "test:int": "test -d test && vitest run --grep '(integration|Integration)' \"test/**/*.{spec,test}.ts\" || echo 'No test directory found, skipping tests'"
  },
  "types": "index.d.ts"
}
