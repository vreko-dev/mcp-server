/**
 * Test Fixtures
 *
 * Shared test data fixtures for consistent testing across the monorepo.
 * Add domain-specific fixtures as needed.
 *
 * @example
 * ```typescript
 * import { mockUser, mockProject } from "@snapback/testing/fixtures";
 *
 * const user = mockUser({ email: "custom@example.com" });
 * ```
 */

// TODO: Add fixtures as needed (users, projects, etc.)
// This is a placeholder for future fixture development

export const PLACEHOLDER_FIXTURES = {
	// Add shared test data here
};
