/**
 * Legacy Tool Handlers
 *
 * These are the original 19 tool handlers copied from apps/mcp-server.
 * They remain unchanged and are kept here for REFERENCE ONLY.
 *
 * ⚠️ EXCLUDED FROM BUILD (see tsconfig.json)
 *
 * These files have imports that reference apps/mcp-server internals.
 * They are kept here as the source of truth for migration.
 *
 * When ready to consolidate:
 * 1. Copy the handler logic to facades/
 * 2. Update imports to use packages/mcp internals
 * 3. Delete these files
 *
 * @module legacy
 */

// NOTE: These exports are for documentation only - this folder is excluded from build
// export * from "./vitals-tools.js";
// export * from "./context-tools.js";
// etc.
